// Interim stub for RealexWrapper during TypeScript migration
// Replaced the large legacy implementation with a minimal runtime-compatible
// object to satisfy imports while we migrate and type the original file.

const RealexWrapper: any = {
  init: () => undefined,
  lightbox: { init: () => undefined },
  embedded: { init: () => undefined },
  redirect: { init: () => undefined },
  setHppUrl: (_: any) => undefined,
  setMobileXSLowerBound: (_: any) => undefined,
  _internal: {},
};

export default RealexWrapper;
